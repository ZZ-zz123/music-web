<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.music.mapper.UserMapper">
    
    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.music.entity.User">
        SELECT * FROM users WHERE username = #{username} AND status = 1
    </select>
    
    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultType="com.music.entity.User">
        SELECT * FROM users WHERE email = #{email} AND status = 1
    </select>
    
    <!-- 根据用户名或邮箱查询用户 -->
    <select id="selectByUsernameOrEmail" resultType="com.music.entity.User">
        SELECT * FROM users 
        WHERE (username = #{usernameOrEmail} OR email = #{usernameOrEmail}) 
        AND status = 1
    </select>
    
</mapper>
